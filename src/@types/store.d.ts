declare namespace App {
  interface Tab {
    title: string
    name: string
  }
}

declare namespace User {
  interface UserInfo {
    name: string
    token: string
  }
}

declare interface Test {
  name: string
  token: string
}
